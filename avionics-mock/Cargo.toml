[package]
name = "avionics-mock"
version = "0.1.0"
edition = "2024"

[dependencies]
protocols = { version = "0.1.0", path = "../protocols" }
rand = "0.9.2"
tokio = { version = "1.48.0", features = ["macros", "rt-multi-thread", "time"] }

[lints]
workspace = true
